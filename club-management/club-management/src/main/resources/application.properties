spring.application.name=club-management
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Database
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/club_management?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# SQL Init Mode
spring.sql.init.mode=always

# AI Configuration (Gemini)
ai.api.key=AIzaSyDuLu8_kZDht8NnoHke4y4qxF55yrZjfK4
ai.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
ai.api.model=gemini-2.5-flash

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# JWT
app.jwt.secret=${JWT_SECRET:clubmanagement_super_secret_key_2025_change_this_in_production}
app.jwt.expiration=86400000
app.qr.secret=${QR_SECRET:qr_attendance_separate_secret_key_2026_change_this_in_production_for_better_security}

# Server
server.port=8080

# WebSocket
spring.websocket.enabled=true

# VNPay Configuration (Sandbox)
# Register at: https://sandbox.vnpayment.vn/devreg
# Replace with your real credentials before going live
vnpay.tmn-code=2QXGZSTR
vnpay.hash-secret=1NBKPMPN7I4ARQRF62RXTAV83INQJ5QK
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.return-url=http://localhost:5173/payments
app.frontend.url=http://localhost:5173

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# SMTP Mail Server Configuration
spring.mail.enabled=${SPRING_MAIL_ENABLED:false}
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
