spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.sql.init.mode=never

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# AI Configuration (OpenAI)
ai.api.key=dummy_key
ai.api.url=https://api.openai.com/v1/chat/completions
ai.api.model=gpt-4o-mini

# JWT
app.jwt.secret=clubmanagement_super_secret_key_2025_change_this_in_production
app.jwt.expiration=86400000
app.qr.secret=qr_attendance_separate_secret_key_2026_change_this_in_production_for_better_security

# Server
server.port=8080

# WebSocket
spring.websocket.enabled=false

# VNPay Configuration (Sandbox)
vnpay.tmn-code=ETD04PPX
vnpay.hash-secret=1NBKPMPN7I4ARQRF62RXTAV83INQJ5QK
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.return-url=http://localhost:8080/api/payment/callback

# SMTP Mail Server (Mock/Test)
spring.mail.host=localhost
